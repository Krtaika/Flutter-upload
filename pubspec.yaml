name: interior_design_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.5.0
  firebase_storage: ^12.3.1
  cloud_firestore: ^5.4.2
  image_picker: ^1.1.2
  image: ^4.2.0 # Ensure this line is present
  provider: ^6.1.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Ensure this line is present

flutter:
  uses-material-design: true